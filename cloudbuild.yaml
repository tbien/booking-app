steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/booking-app:$COMMIT_SHA'
      - '.'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/booking-app:$COMMIT_SHA'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'booking-app'
      - '--image=gcr.io/$PROJECT_ID/booking-app:$COMMIT_SHA'
      - '--region=europe-west1'
      - '--platform=managed'
      - '--set-env-vars=NODE_ENV=production'
      - '--set-secrets=MONGODB_URI=MONGODB_URI:latest,SESSION_SECRET=SESSION_SECRET:latest'
      - '--quiet'

images:
  - 'gcr.io/$PROJECT_ID/booking-app:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY
